import { style } from '@vanilla-extract/css';

export const mapViewerContainer = style({
  width: '100%',
  height: '100%',
  position: 'relative',
});

export const mapViewerCanvas = style({
  width: '100%',
  height: '100%',
});
